<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/">
    <channel>
        <title>My Site Blog</title>
        <link>https://reantoilpc.github.io/blog</link>
        <description>My Site Blog</description>
        <lastBuildDate>Fri, 29 May 2026 00:00:00 GMT</lastBuildDate>
        <docs>https://validator.w3.org/feed/docs/rss2.html</docs>
        <generator>https://github.com/jpmonette/feed</generator>
        <item>
            <title><![CDATA[WSL2 + Claude CLI 記憶體越用越多最後當機 — 完整除錯與解法]]></title>
            <link>https://reantoilpc.github.io/blog/2026-05-29-wsl2-claude-cli-memory</link>
            <guid>2026-05-29-wsl2-claude-cli-memory</guid>
            <pubDate>Fri, 29 May 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[TL;DR]]></description>
            <content:encoded><![CDATA[<h2>TL;DR</h2><p>在 Windows 11 用 WSL2 Ubuntu 跑 Claude CLI，Task Manager 看到 <code>VmmemWSL</code> 一路漲、最後整機卡死。<strong>真正根因</strong>不是 Claude CLI 漏記憶體，而是三件事疊加：</p><ol><li>舊的 <code>claude --resume</code> session 沒關，殘留在背景吃記憶體</li><li>每個 session 都會起一組 MCP server（Azure / Atlassian / …），數量乘以 session 數</li><li><strong>沒設 <code>.wslconfig</code></strong>，WSL2 既沒有 memory cap，也沒開 <code>autoMemoryReclaim</code>，閒置 RAM 不會還給 Windows</li></ol><p><strong>解法</strong>：殺殘留 session → <code>wsl --shutdown</code> → 建 <code>.wslconfig</code>（memory cap + <code>autoMemoryReclaim=gradual</code> + <code>sparseVhd=true</code>）。</p><h2>1. 問題現象</h2><table><thead><tr><th>現象</th><th>數據</th></tr></thead><tbody><tr><td>Windows 整機記憶體</td><td><strong>89%</strong> 已用</td></tr><tr><td>VmmemWSL（WSL2 VM process）</td><td>2,627 MB</td></tr><tr><td>WSL 越用越多，最後當掉</td><td>OOM killer 殺進程 + Windows 自己 swap</td></tr></tbody></table><p>Windows Task Manager 記憶體分佈：</p><pre><code class="language-text">VmmemWSL              2,627.9 MB
Google Chrome (13)      832.1 MB
JetBrains Rider         758.3 MB
Microsoft Teams (10)    549.3 MB
遠端桌面連線            317.8 MB
JetBrains Rider.Backend 289.3 MB
Google Chrome           272.1 MB
Postman (5)             251.4 MB
</code></pre><hr/><h2>2. 排查：先看 WSL 內部實況</h2><pre><code class="language-bash">$ free -h
               total        used        free      shared  buff/cache   available
Mem:            15Gi       3.1Gi        11Gi       114Mi       759Mi        12Gi
Swap:          4.0Gi          0B       4.0Gi
</code></pre><p>關鍵觀察：</p><ul><li><code>Mem total = 15Gi</code> → <strong>WSL 看得到主機全部 RAM</strong>，代表沒設 memory cap</li><li><code>buff/cache = 759 MB</code> → 不是 page cache 漲爆</li><li><code>used 3.1 Gi</code> 對得上 Windows 端看到的 <code>VmmemWSL 2.6 GB</code></li></ul><p>確認沒有 <code>.wslconfig</code>：</p><pre><code class="language-bash">$ ls /mnt/c/Users/rayhsu/.wslconfig
ls: cannot access &#x27;/mnt/c/Users/rayhsu/.wslconfig&#x27;: No such file or directory
</code></pre><hr/><h2>3. 證明：找出誰在吃記憶體</h2><pre><code class="language-bash">$ ps aux --sort=-%mem | head -10
USER     PID %CPU %MEM     VSZ    RSS TTY STAT START   TIME COMMAND
rayhsu  3262  0.8  4.5 7490M   738M pts/4 Sl+ May28  7:24 claude --resume --effort=max
rayhsu  2055  0.7  4.1 7490M   671M pts/3 Sl+ May28  7:02 claude --resume --effort=max
rayhsu  2405  0.6  3.4 7483M   556M pts/2 Sl+ May28  5:28 claude --resume --effort=max
rayhsu 67652  3.7  2.6 7624M   431M pts/5 Sl+ 09:12  0:56 claude --resume xxxx
rayhsu  3455  0.0  0.8 3645M   138M pts/4 Sl+ May28  0:27 azmcp server start ...
rayhsu  2599  0.0  0.8 3644M   135M pts/2 Sl+ May28  0:28 azmcp server start ...
rayhsu  2247  0.0  0.8 3644M   132M pts/3 Sl+ May28  0:28 azmcp server start ...
</code></pre><p><strong>找到了</strong>：</p><ul><li><strong>4 個並行的 <code>claude --resume</code> session</strong>，三個是昨天（<code>May28</code>）開的沒關 → RSS 加總 ~2.4 GB</li><li><strong>每個 session 各自起一組 MCP server</strong>（<code>azmcp</code> ~130 MB + npm wrapper ~90 MB = ~220 MB），4 組 × 220 MB ≈ ~880 MB</li><li>VSZ 巨大（7.4 GB）是 Node.js V8 預留的 virtual address space，<strong>不代表實際使用</strong>；看 RSS 才是真實</li></ul><hr/><h2>4. 三個放大記憶體的根因</h2><h3>A. 殘留的 claude session</h3><p>Claude CLI 是 Node.js 寫的，<strong>長時間運行的 session 會緩慢長 heap</strong>：</p><ul><li>conversation transcript 留在記憶體</li><li>tool result buffer（讀進來的檔案、圖片）累積</li><li>即使 session 自動 compact，process heap 也不會立刻釋放</li></ul><p>→ <strong><code>/exit</code> 才算真的關閉</strong>；關掉終端機視窗如果是 detached 模式 process 還在。</p><h3>B. 每個 session 都吊一組 MCP server</h3><p>MCP server 是 <code>claude</code> 的 subprocess，<strong>claude 死了它才會死</strong>。所以：</p><pre><code class="language-text">N 個 claude session × M 個 MCP server = N × M 個 Node process
</code></pre><p>我這台同時掛 4 個 session，每個都起 Azure MCP（即使這個專案根本沒用到 Azure）→ 浪費 ~880 MB。</p><h3>C. WSL2 沒 memory cap、不主動回收</h3><p>Windows 11 上 WSL2 預設 <code>memory = 主機全部 RAM</code>，再加上 <strong>Hyper-V 不會主動把閒置記憶體還給 Windows</strong>（dynamic memory 是針對 Linux kernel 看不到的層級）。結果：</p><blockquote><p><strong>WSL 記憶體峰值會留住，下不來 → Windows 越來越緊</strong></p></blockquote><p>這就是「越用越多」感覺的物理機制。</p><hr/><h2>5. 為什麼會「當掉」</h2><p>兩條路徑：</p><pre><code class="language-text">路徑 1（WSL 內爆）：
  WSL anon memory ↗ → 接近 cap → Linux OOM killer 殺進程
                                   └─ claude / mcp / build 突然消失

路徑 2（Windows 整機爆）：
  VmmemWSL 持續高水位 + Chrome/Rider/Teams 一起吃
  → Windows 開始 swap → 整機 freeze 感
</code></pre><p>兩條路徑都會體感「當機」，但<strong>原因不同、徵兆不同</strong>。</p><hr/><h2>6. 解法</h2><h3>步驟 1：先殺殘留 session（立刻見效）</h3><pre><code class="language-bash"># 看誰是舊的（從昨天留下來的）
ps -ef | grep &quot;claude --resume&quot; | grep -v grep

# 留新的，殺舊的（你的 PID 會不同）
kill 2055 2405 3262
</code></pre><p>→ 立刻釋放約 2 GB（claude 本身 + 跟它跑的 MCP server）。</p><h3>步驟 2：<code>wsl --shutdown</code> 讓 Windows 收回記憶體</h3><p>在 <strong>PowerShell</strong>（不是 WSL 視窗）：</p><pre><code class="language-powershell">wsl --shutdown
</code></pre><blockquote><p>注意：這會關掉所有 WSL 視窗。下次 <code>wsl</code> 進來時記憶體歸零、<code>VmmemWSL</code> 從 0 開始計。</p></blockquote><h3>步驟 3：建 <code>.wslconfig</code> 治本</h3><p>放在 <strong>Windows 端</strong>（不是 Linux 裡）：</p><pre><code class="language-text">C:\Users\&lt;user&gt;\.wslconfig
</code></pre><p>從 WSL 端寫等同寫到 <code>/mnt/c/Users/&lt;user&gt;/.wslconfig</code>，是同一個檔。</p><p>完整內容：</p><pre><code class="language-ini"># WSL2 global config. Applies to all distros. Edit + `wsl --shutdown` to reload.

[wsl2]
# Cap WSL memory at 8GB (host has 16GB; leaves room for Windows + Rider + Chrome + Teams).
memory=8GB

# Swap inside the WSL VM. Acts as a safety net before OOM killer fires.
swap=4GB

# CPU cores given to WSL (host has 20). 8 is plenty for parallel claude sessions
# without starving Windows-side IDE / browser.
processors=8

# Default true on Win11, but explicit beats implicit when you debug later.
localhostForwarding=true

[experimental]
# Auto-release freed memory back to Windows. Without this, VmmemWSL keeps the
# high-water mark forever — the root cause of &quot;WSL grows but never shrinks&quot;.
# Options: disabled | gradual | dropcache. `gradual` is the safe default.
autoMemoryReclaim=gradual

# Shrink the ext4 vhdx on file delete (otherwise it grows monotonically).
sparseVhd=true
</code></pre><blockquote><p>編輯後一定要 <code>wsl --shutdown</code> 才會重新讀取。</p></blockquote><hr/><h2>7. <code>.wslconfig</code> 各 key 在幹什麼</h2><h3><code>[wsl2]</code> section</h3><table><thead><tr><th>Key</th><th>作用</th><th>不設的後果</th></tr></thead><tbody><tr><td><code>memory=8GB</code></td><td>WSL VM 記憶體上限</td><td>Win11 預設 = 主機全部 RAM，會把 Windows 擠爆</td></tr><tr><td><code>swap=4GB</code></td><td>WSL VM 內 swap 大小</td><td>預設 = 25% RAM；設 0 = 沒安全網，一爆 cap 直接 OOM kill</td></tr><tr><td><code>processors=8</code></td><td>WSL 看到幾顆 vCPU</td><td>預設 = 主機全部核心，build 跑滿時拖到 Windows</td></tr><tr><td><code>localhostForwarding=true</code></td><td>WSL 服務 listen 在 0.0.0.0 → Windows <code>localhost:port</code> 可連</td><td>Win11 預設 true；寫上方便除錯</td></tr></tbody></table><h3><code>[experimental]</code> section（要分開放）</h3><table><thead><tr><th>Key</th><th>作用</th><th>不設的後果</th></tr></thead><tbody><tr><td><code>autoMemoryReclaim=gradual</code></td><td>Linux 釋放 page 後逐步把 RAM 還給 Windows</td><td>VmmemWSL 永遠停在歷史高水位</td></tr><tr><td><code>sparseVhd=true</code></td><td>ext4 vhdx 變 sparse 格式，刪檔可縮</td><td>vhdx 只長不縮，磁碟用久越腫</td></tr></tbody></table><p><code>autoMemoryReclaim</code> 三個選項：</p><ul><li><code>disabled</code>：不還（舊預設）</li><li><code>gradual</code>：慢慢還，保留 page cache（<strong>推薦</strong>）</li><li><code>dropcache</code>：激進還，連 page cache 也丟，下次讀檔要重抓 disk</li></ul><hr/><h2>8. 踩到的坑 — 放錯 section 會被忽略</h2><p>第一次設定我把 <code>autoMemoryReclaim</code> 跟 <code>sparseVhd</code> 放在 <code>[wsl2]</code> 底下，重啟 WSL 時 PowerShell 印：</p><pre><code class="language-text">wsl: wsl2.autoMemoryReclaim:C:\Users\rayhsu\.wslconfig 中不明的按鍵 &#x27;17&#x27;
wsl: wsl2.sparseVhd:C:\Users\rayhsu\.wslconfig 中不明的按鍵 &#x27;20&#x27;
</code></pre><p>訊息拆解：</p><ul><li><code>wsl2.autoMemoryReclaim</code> → 在 <code>[wsl2]</code> section 找這個 key</li><li><code>中不明的按鍵</code> → 中文翻譯怪：意思是「不認得這個 key」（key 被翻成「按鍵」）</li><li><code>&#x27;17&#x27;</code> / <code>&#x27;20&#x27;</code> → <strong>是 <code>.wslconfig</code> 的行號</strong>，剛好對到那兩行</li></ul><p><strong>修正</strong>：把這兩個 key 移到 <code>[experimental]</code> section。</p><pre><code class="language-diff"> [wsl2]
 memory=8GB
 swap=4GB
 processors=8
 localhostForwarding=true
-autoMemoryReclaim=gradual
-sparseVhd=true
+
+[experimental]
+autoMemoryReclaim=gradual
+sparseVhd=true
</code></pre><blockquote><p>這兩個功能在 WSL 還屬於「實驗性」，Microsoft 要求放 <code>[experimental]</code> 底下。</p></blockquote><hr/><h2>9. <code>.wslconfig</code> 放在哪？兩個概念分清楚</h2><table><thead><tr><th>檔案</th><th>位置</th><th>何時讀</th><th>範圍</th></tr></thead><tbody><tr><td><strong><code>.wslconfig</code></strong></td><td>Windows <code>%UserProfile%\.wslconfig</code>（= <code>C:\Users\&lt;user&gt;\.wslconfig</code>）</td><td>Linux VM <strong>啟動前</strong>，由 Windows 上的 WSL 服務讀</td><td>全域（所有 distro）</td></tr><tr><td><strong><code>/etc/wsl.conf</code></strong></td><td>Linux 內 <code>/etc/wsl.conf</code></td><td>Linux 已經開機後</td><td>該 distro 限定</td></tr></tbody></table><p><code>memory=8GB</code> 這種設定是「<strong>Hyper-V 要分配給 VM 多少資源</strong>」 → Linux 還沒開機之前 Windows 就要決定 → 必須在 Windows host 上。</p><p><strong>從 WSL 寫和從 Windows 寫是同一個檔</strong>：</p><pre><code class="language-text">Windows: C:\Users\rayhsu\.wslconfig          ┐
                                              ├─ 同一個檔案，兩個視角
WSL:     /mnt/c/Users/rayhsu/.wslconfig      ┘
</code></pre><p><code>/mnt/c/</code> 是 WSL 自動掛載的 Windows C 槽橋接點。</p><hr/><h2>10. 驗證清單</h2><h3>不用重啟就能驗</h3><pre><code class="language-bash"># A. 檔案有沒有寫對
cat /mnt/c/Users/&lt;user&gt;/.wslconfig

# B. memory cap 有沒有吃到（找 Mem total 應該 ~7.8 Gi）
free -h

# C. processors cap 有沒有吃到（應該 = 8）
nproc

# D. ini 語法自驗（Python configparser）
python3 -c &quot;
import configparser
c = configparser.ConfigParser()
c.read(&#x27;/mnt/c/Users/&lt;user&gt;/.wslconfig&#x27;)
for s in c.sections():
    print(f&#x27;[{s}]&#x27;)
    for k, v in c.items(s):
        print(f&#x27;  {k} = {v}&#x27;)
&quot;
</code></pre><h3>需要 <code>wsl --shutdown</code> 重啟後才能驗</h3><pre><code class="language-bash"># E. [experimental] section 有沒有被認得（重點）
#    重啟後 PowerShell 跑 wsl 進來，不應該再印「不明的按鍵」warning
wsl.exe -e true 2&gt;&amp;1
# 預期輸出：空白
</code></pre><pre><code class="language-powershell"># F. sparseVhd 有沒有真的 sparse
#    比較 Windows 端 vhdx 實體大小 vs Linux 內部 used
Get-ChildItem &#x27;C:\Users\&lt;user&gt;\AppData\Local\wsl\&lt;guid&gt;\ext4.vhdx&#x27; |
    Select-Object Name, @{N=&#x27;GB&#x27;;E={[math]::Round($_.Length/1GB,2)}}
</code></pre><pre><code class="language-text"># G. autoMemoryReclaim 行為
#    跑一波吃 RAM 的工作後，過 5~10 分鐘，Windows Task Manager 看 VmmemWSL 應緩降
#    （無法瞬間驗，要實際 workload）
</code></pre><h3>實際驗證結果</h3><table><thead><tr><th>#</th><th>檢查</th><th>結果</th></tr></thead><tbody><tr><td>A</td><td><code>.wslconfig</code> 內容、行數</td><td>✓ 879 bytes</td></tr><tr><td>B</td><td><code>Mem total = 7.8 Gi</code></td><td>✓</td></tr><tr><td>C</td><td><code>nproc = 8</code></td><td>✓</td></tr><tr><td>D</td><td>ini 自驗：兩個 section + 6 個 key 全 parse 通過</td><td>✓</td></tr><tr><td><strong>E</strong></td><td><strong><code>wsl.exe -e true</code> stderr 完全空白</strong></td><td>✓✓（最關鍵）</td></tr><tr><td>F</td><td>Linux used 31 GB vs vhdx 35.16 GB（差 4 GB metadata）</td><td>✓</td></tr><tr><td>G</td><td>autoMemoryReclaim 行為</td><td>⏳ 待後續觀察</td></tr></tbody></table><hr/><h2>11. 心智模型</h2><pre><code class="language-text">Windows 16GB RAM
       │
       ├── Windows + Chrome + Rider + Teams …  (~5~7 GB)
       │
       └── WSL2 VM (cap = 8GB) ─┐
                  │             ├ Linux kernel ~400 MB
                  │             ├ claude session A (~700 MB)
                  │             ├ claude session B (~500 MB)
                  │             ├ MCP servers (~200 MB × N)
                  │             ├ bun dev / build
                  │             └ swap (4GB, vhdx in %TEMP%)
                  │
                  └─[gradual reclaim]→ 閒置時慢慢歸還
</code></pre><p><strong>沒設這份 config 之前</strong>：最後那條 reclaim 箭頭不存在 → 高水位永遠不下來，這就是 89% 的成因。</p><hr/><h2>12. 養成的好習慣</h2><ul><li>用完 <code>/exit</code> 收掉 claude session，不要把終端機留著</li><li>不要 4 個 session 同時開 <code>--effort=max</code>；max 模式吃 CPU 又拖長 session 自然吃更多 heap</li><li>不用的 MCP server 在 <code>~/.claude.json</code> 或專案 <code>settings.json</code> 裡關掉（Azure MCP 在非 Azure 專案就關）</li><li>定期 <code>wsl --shutdown</code> 重置 VM</li><li>Windows Task Manager 釘住「記憶體」欄位排序，VmmemWSL 飆超過 cap 一半就該收 session 了</li></ul><hr/><h2>13. 補充：常用診斷指令</h2><pre><code class="language-bash"># 看哪個 process 吃最多記憶體
ps aux --sort=-%mem | head -10

# 看所有 claude 進程 + RSS 加總
ps -o rss= -C claude | awk &#x27;{sum+=$1} END {printf &quot;%.0f MB across %d procs\n&quot;, sum/1024, NR}&#x27;

# 看 VM uptime（確認是新啟動的 VM 還是舊的）
uptime

# 找 vhdx 實體位置
find /mnt/c/Users/&lt;user&gt;/AppData/Local -maxdepth 5 -name &quot;ext4.vhdx&quot; 2&gt;/dev/null

# WSL 版本（autoMemoryReclaim 需要 0.66.1+，sparseVhd 需要 1.0+）
wsl.exe --version
</code></pre><pre><code class="language-powershell"># Windows 端：強制重置 WSL VM（釋放 VmmemWSL 回 Windows）
wsl --shutdown

# 看目前哪些 distro 在跑
wsl --list --running
</code></pre><hr/><h2>14. 參考資料</h2><ul><li><a href="https://learn.microsoft.com/zh-tw/windows/wsl/wsl-config">WSL <code>.wslconfig</code> 官方文件</a></li><li><a href="https://github.com/microsoft/WSL/releases"><code>autoMemoryReclaim</code> 發布筆記（WSL 0.66.1）</a></li><li><a href="https://learn.microsoft.com/zh-tw/windows/wsl/disk-space">Sparse VHD support in WSL</a></li></ul><hr/><h2>附錄：本部落格發文流程備忘（給未來的我）</h2><p>這個 blog 是 Docusaurus 2.0.0-beta.14（2021 年）+ GitHub Pages，source repo 跟 deploy repo 分開：</p><ul><li><strong>Source</strong>：<a href="https://github.com/reantoilpc/docusaurus_websit"><code>reantoilpc/docusaurus_websit</code></a> — 寫 markdown 的地方</li><li><strong>Deploy 產出</strong>：<a href="https://github.com/reantoilpc/reantoilpc.github.io"><code>reantoilpc/reantoilpc.github.io</code></a> — 編譯後靜態檔案</li></ul><p>原本 CI（<code>.github/workflows/publish-blog.yml</code>）會在 push 到 main 後自動 build + deploy，但 <strong>CI 目前壞掉</strong>（後面會解釋），所以暫時用「本機 build + deploy」繞過。</p><h3>標準發文流程（CI 壞掉期間）</h3><pre><code class="language-bash">cd ~/projects/Github/docusaurus_websit

# 1. 寫新文章
# 檔名格式：blog/YYYY-MM-DD-slug.md
# frontmatter 範本：
# ---
# slug: YYYY-MM-DD-slug
# title: 文章標題
# authors: rayhsu
# tags: [tag1, tag2]
# ---

# 2. commit source
git add blog/&lt;新檔&gt;.md
git commit -m &quot;&lt;簡短中文敘述&gt;&quot;
git push origin main

# 3. 本機 build &amp; deploy（繞 CI）
GIT_USER=reantoilpc DEPLOYMENT_BRANCH=main USE_SSH=false npx docusaurus deploy

# 4. 等 GitHub Pages CDN propagate（30~60 秒）
curl -sI https://reantoilpc.github.io/blog/YYYY-MM-DD-slug/ | head -1
# 預期：HTTP/2 200
</code></pre><h3>CI 為什麼壞掉</h3><p>問題在 <code>package.json</code> 用 caret ranges（<code>^2.0.0-beta.14</code>）+ <strong>repo 沒 yarn.lock</strong>：</p><ol><li>CI 跑 <code>yarn install</code> 每次都抓最新 transitive deps</li><li>套件升版後跟 Docusaurus 2021 年的版本飄走，build 工具鏈（webpack ProgressPlugin、node-releases…）API mismatch</li><li>連續兩次 fail：<ul><li>第一次：<code>node-releases@2.0.46</code> 要求 Node ≥18，CI 跑 Node 14 → 已修（bump 到 Node 18）</li><li>第二次：webpack ProgressPlugin schema 不認得 <code>name</code> / <code>color</code> / <code>reporters</code> 屬性 → 沒修</li></ul></li></ol><p>本機之所以能 build，是因為 <code>node_modules/</code> 被 commit 在 repo 裡（290 MB），webpack 鎖在相容的 5.67.0。</p><h3>之後想根治 CI 的三條路</h3><table><thead><tr><th>路線</th><th>動作</th><th>風險</th></tr></thead><tbody><tr><td>(a) 用既有 node_modules 生 yarn.lock</td><td><code>yarn import</code> 從 <code>package-lock.json</code> 轉、或 <code>synp</code> 工具</td><td>中（要驗轉換正確性）</td></tr><tr><td>(b) workflow 改用 <code>npm ci</code> + 既有 <code>package-lock.json</code></td><td>改一行 + lockfile 也老，可能還要鎖 webpack <code>resolutions</code></td><td>中（package-lock.json 也是 4 年前）</td></tr><tr><td>(c) 升級到 Docusaurus 3</td><td>大改，可能撞 mdx 語法、theme override、React 17→18 等</td><td>高</td></tr></tbody></table><p>目前 trade-off：每篇文章手動跑一次 <code>npx docusaurus deploy</code> 不算慢（本機既有 node_modules，build 約 20 秒），治本工程量大，等下次想動再說。</p><h3>環境檢查清單</h3><p>換新電腦 / 重 clone 後要先確認本機能 build：</p><pre><code class="language-bash">cd ~/projects/Github/docusaurus_websit
# node_modules 應該已經 commit 進 repo（290 MB）
ls node_modules/webpack/package.json &amp;&amp; cat node_modules/webpack/package.json | grep version
# 預期 &quot;version&quot;: &quot;5.67.0&quot;

# 試一次 build 不 deploy
npx docusaurus build
# 預期：Generated static files in build. (+ build/en/)
</code></pre><p>如果 build 過了就放心。Deploy 走 <code>USE_SSH=false</code> 用 HTTPS + <code>gh auth</code> 既有憑證，不需要額外 SSH key。</p><hr/><p><em>記錄日期：2026-05-29 / 環境：Windows 11 build 26200, WSL 2.6.3.0, Ubuntu 24.04</em></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[使用 Lens IDE 管理 Azure K8s]]></title>
            <link>https://reantoilpc.github.io/blog/2023-03-25-lens-with-k8s</link>
            <guid>2023-03-25-lens-with-k8s</guid>
            <pubDate>Sat, 25 Mar 2023 00:00:00 GMT</pubDate>
            <description><![CDATA[起因]]></description>
            <content:encoded><![CDATA[<h2>起因</h2><p>最近公司開始導入微服務概念後，工作上的維運也開始有些轉變，系統的佈署從原本的地端改成Azure雲端，並且打包成<code>Container</code>佈署在<code>k8s</code>上，因此就需要擴充工作上的技能包，最先要提升就是k8s觀念及如何維運在k8s上運行的專案。</p><p>所以要怎麼透過工具連到<code>Azure k8s</code>就變成很重要。</p><h2>Lens 工具</h2><p>Step1
先註冊帳號，並下載 <a href="https://k8slens.dev/">Lens</a></p><p>Setp2
由於 k8s 是安裝 Azure 服務上，因此可以透過下指令方式對 Azure 服務操作，下載安裝 <a href="https://learn.microsoft.com/zh-tw/cli/azure/">Azure CLI</a></p><p>Setp3
由於要透過 IDE 連線 k8s cluster 就必需要有 k8s config 內包含 Cluster、User、Namespace 與身分認證相關資訊，因此要透過 Azure CLI 進行 k8s config 匯出</p><ul><li>開起 PowserSell 下 Azure CLI 指令</li></ul><pre><code class="language-shell">
# step 1 登入 Azure
az login

# step 2 取得憑證，會下載憑證設定，Azure 已產生好語法，只要參考下圖，複製指令執行就可以
az aks get-credentials --resource-group &lt;resource group name&gt; --name &lt;k8s name&gt;

</code></pre><p><img src="./pictures/2023-03-25/download-k8s-config.png" alt="image"/></p><ul><li>匯出 k8s config 檔案會放至 <code>C:\Users\{User}\.kube</code>，格式大至如下</li></ul><pre><code class="language-yml">apiVersion: v1
clusters:
- cluster:
    certificate-authority-data: &lt;ca-data-here&gt;
    server: https://your-k8s-cluster.com
  name: &lt;cluster-name&gt;
contexts:
- context:
    cluster:  &lt;cluster-name&gt;
    user:  &lt;cluster-name-user&gt;
  name:  &lt;cluster-name&gt;
current-context:  &lt;cluster-name&gt;
kind: Config
preferences: {}
users:
- name:  &lt;cluster-name-user&gt;
  user:
    token: &lt;secret-token-here&gt;
</code></pre><p>Setp4</p><p>Lens 讀取 k8s config</p><p><img src="./pictures/2023-03-25/lens-import-config.png" alt="image"/></p><p>讀取成功畫面</p><p><img src="./pictures/2023-03-25/lens-connection-k8s.png" alt="image"/></p><p>找到系統佈署的Pods</p><p><img src="./pictures/2023-03-25/lens-with-pods.png" alt="image"/></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[建立 visual studio multiple project template]]></title>
            <link>https://reantoilpc.github.io/blog/2023-02-22-Create-Multiple-Project-Template</link>
            <guid>2023-02-22-Create-Multiple-Project-Template</guid>
            <pubDate>Wed, 22 Feb 2023 00:00:00 GMT</pubDate>
            <description><![CDATA[起因]]></description>
            <content:encoded><![CDATA[<h2>起因</h2><p>隨著工作的經驗增加，接手的案子也越來越多，每個案子使用系統的架構大同小異，但每次開一個新的案子，都需要重新設定或是重新安裝套件，雖然只有一開始才需要設定，但幾次下來也是花了不少時間。</p><p>因此這次打算使用 visual studio 匯出專案功能來建立系統範本，使用範本可以大大減少初始系統的設定和安裝套件的時間。</p><h2>專案匯出</h2><ul><li>先建立要使用的系統架構，並安裝相關套件</li></ul><p><img src="./pictures/2023-02-22/2023-02-22-create-project.png" alt="image"/></p><ul><li>轉換成專案範本，namespace 要使用系統名稱</li></ul><pre><code class="language-CSharp">
// 原本的 namepspace
namespace PMP.EdgeService.Api.Controllers;

// 使用範本命名 
namespace $ext_safeprojectname$.Controllers;

</code></pre><p><a href="https://learn.microsoft.com/en-us/visualstudio/ide/template-parameters?view=vs-2022">參數相關設定</a></p><p><img src="./pictures/2023-02-22/2023-02-22-setting-namespace.png" alt="image"/></p><ul><li>選擇專案匯出範本</li></ul><p><img src="./pictures/2023-02-22/2023-02-22-export-project.png" alt="image"/></p><p><img src="./pictures/2023-02-22/2023-02-22-select-project.png" alt="image"/></p><ul><li>預設 Automatically Import the template into visual studio 打勾要取消，不要打勾</li></ul><p><img src="./pictures/2023-02-22/2023-02-22-setting-template.png" alt="image"/></p><p>匯出的 zip 檔，預設路徑為 <code>C:\Users\{使用者名稱}\Documents\Visual Studio 2022\Templates\ProjectTemplates</code></p><p><img src="./pictures/2023-02-22/2023-02-22-template-path.png" alt="image"/></p><h2>設定 template 設定</h2><ul><li>新增一個資料夾，把匯出各別專案範本 zip 檔解壓縮後放置進去。</li><li>建立系統範本的設定檔，副檔名為<code>vstemplate</code></li></ul><p><img src="./pictures/2023-02-22/2023-02-22-create-vstemplate.png" alt="image"/></p><ul><li>系統範本的設定檔內容如下</li></ul><pre><code class="language-xml">
&lt;VSTemplate xmlns=&quot;http://schemas.microsoft.com/developer/vstemplate/2005&quot; Version=&quot;3.0.0&quot; Type=&quot;ProjectGroup&quot;&gt;
    &lt;TemplateData&gt;
        &lt;Name&gt;Web Template&lt;/Name&gt;
        &lt;Description&gt;Project template creating ASP.NET 6 web apps and web APIs for windows, Lunix, macOS &lt;/Description&gt;
        &lt;ProjectType&gt;CSharp&lt;/ProjectType&gt;
        &lt;LanguageTag&gt;C#&lt;/LanguageTag&gt;
        &lt;PlatformTag&gt;Windows&lt;/PlatformTag&gt;
        &lt;ProjectTypeTag&gt;Web&lt;/ProjectTypeTag&gt;
        &lt;CreateNewFolder&gt;true&lt;/CreateNewFolder&gt;
        &lt;LocationField&gt;Enabled&lt;/LocationField&gt;
    &lt;/TemplateData&gt;
    &lt;TemplateContent&gt;
        &lt;ProjectCollection&gt;
            &lt;SolutionFolder Name=&quot;Src&quot;&gt;
                &lt;ProjectTemplateLink ProjectName=&quot;$projectname$.Api&quot; CopyParameters=&quot;true&quot;&gt;Template.Api\MyTemplate.vstemplate&lt;/ProjectTemplateLink&gt;
                &lt;ProjectTemplateLink ProjectName=&quot;$projectname$.Application&quot; CopyParameters=&quot;true&quot;&gt;Template.Application\MyTemplate.vstemplate&lt;/ProjectTemplateLink&gt;                
            &lt;/SolutionFolder&gt;            
        &lt;/ProjectCollection&gt;
    &lt;/TemplateContent&gt;
&lt;/VSTemplate&gt;

</code></pre><h2>建立系統範本</h2><ul><li>把專案的資料夾和系統範本的設定檔一起壓縮成一個 zip 檔</li><li>把 zip 放置 visual studio 讀取範本的路徑位置</li><li>不知道路徑可以在 tools &gt; options &gt; projects and solutions &gt; locations 找到對應路徑 User project template location</li></ul><p><img src="./pictures/2023-02-22/2023-02-22-find-template-path.png" alt="image"/></p><ul><li>確認系統範本</li></ul><p><img src="./pictures/2023-02-22/2023-02-22-system-template.png" alt="image"/></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[miniProfiler 使用介紹]]></title>
            <link>https://reantoilpc.github.io/blog/2022-12-29-miniProfiler-guide</link>
            <guid>2022-12-29-miniProfiler-guide</guid>
            <pubDate>Thu, 29 Dec 2022 00:00:00 GMT</pubDate>
            <description><![CDATA[使用原因]]></description>
            <content:encoded><![CDATA[<h2>使用原因</h2><p>最近協助團隊優化排程執行效率，想要解決執行效率不好問題，就需要知道問題出在那個環節才能進行修正。過去在處理這類問題，都是用肉眼看程式碼和經驗來判斷，而這樣方式很沒有效率，而且不一定改對方向，反而花大量時間卻沒有解決真正問題。</p><p>為了有效率的找出問題點在那裡，導入<code>miniProfiler</code>該套件，該套件不止可以有效記錄程式執行所花費時間，更可以記錄SQL執行花費時間及產生相對的語法，方便調整SQL語法。</p><h2>MiniProfiler 介紹</h2><p>MiniProfiler 是一個專門分析應用系統和UI介面的工具，透過工具可以解析出程式所花費時間，以及執行那些程式碼，有這些內容可以幫助我們調整優化系統效能。</p><h2>如何使用</h2><p>安裝相關套件</p><ul><li>MiniProfiler.AspNetCore</li><li>MiniProfiler.AspNetCore.Mvc</li><li>MiniProfiler.EntityFrameworkCore</li></ul><p><a href="https://localhost:64137/profiler/results-index">監控路徑</a></p><p>Startup.cs 設定</p><pre><code class="language-C#">
    services.AddMiniProfiler(options =&gt;
    {
        //訪問地址路由根目錄；預設為：/mini-profiler-resources
        options.RouteBasePath = &quot;/profiler&quot;;
        //資料快取時間
        (options.Storage as MemoryCacheStorage).CacheDuration = TimeSpan.FromMinutes(3);
        //sql格式化設定
        options.SqlFormatter = new InlineFormatter();
        //跟蹤連線開啟關閉
        options.TrackConnectionOpenClose = true;
        //介面主題顏色方案;預設淺色
        options.ColorScheme = ColorScheme.Dark;
        //.net core 3.0以上：對MVC過濾器進行分析
        // options.EnableMvcFilterProfiling = true;
        //對檢視進行分析
        // options.EnableMvcViewProfiling = true;

        //控制訪問頁面授權，預設所有人都能訪問
        //options.ResultsAuthorize;
        //要控制分析哪些請求，預設說有請求都分析
        //options.ShouldProfile;

        //內部異常處理
        //options.OnInternalError = e =&gt; MyExceptionLogger(e);
        options.IgnorePath(&quot;/health&quot;);
        options.IgnorePath(&quot;/swagger&quot;);

    }).AddEntityFramework();
    
    
    app.UseMiniProfiler();

</code></pre><h2>用法</h2><pre><code class="language-C#">    var miniProfiler = MiniProfiler.StartNew(&quot;更新服務的支付工具&quot;);
    using (miniProfiler.Step(&quot;RefreshSubscribedService&quot;))
    {
        // 監控的程式碼
    }
    miniProfiler.Stop();
</code></pre><h2>使用套件</h2><p>使用 AOP 套件建立 MiniProfiler.Extensions 套件</p><pre><code class="language-C#">
    [Profiler(&quot;Test&quot;,&quot;RefreshSubscribedService&quot;)]
    public async Task&lt;bool&gt; RefreshSubscribedService(string Id)
    {
        
    }

</code></pre>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[github unitofwork 使用介紹]]></title>
            <link>https://reantoilpc.github.io/blog/2022-11-02-github-unitofwork</link>
            <guid>2022-11-02-github-unitofwork</guid>
            <pubDate>Wed, 02 Nov 2022 00:00:00 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[efcore 使用介紹]]></title>
            <link>https://reantoilpc.github.io/blog/2022-08-17-efcore-guide</link>
            <guid>2022-08-17-efcore-guide</guid>
            <pubDate>Wed, 17 Aug 2022 00:00:00 GMT</pubDate>
            <description><![CDATA[EFCore 建議寫法]]></description>
            <content:encoded><![CDATA[<h2>EFCore 建議寫法</h2><h3>Repository 回傳</h3><p>Repository 設計概念，希望透過傳入條件值篩選資料出來。如果單純查詢一個資料表這樣寫法沒有問題，但通常在顯示或是處理資料，不會只有查一個資料表，往往都要查詢好幾個資料表，進行 Join 後才是我們要的結果。</p><p>如果 Repository 回傳是 IEnumerable、List、Array 這些類型，等同於立刻執行一段 SQL 語法，意思就是 Join 幾個資料表就執行幾次 SQL 語法，這樣寫法是很消耗資料庫效能，而且執行效率也比較慢，因為是分別 Query 資料表並沒有使用到 SQL Join 功能。</p><p>在 EFCore 可以使用 IQueryable 進行延遲執行取得資料，意思就是無論 Join 幾個資料表，都還不會Query 資料表，而是等到 ToListAsync ，EFCore 才會產生相對應的 SQL 語法，這時候就會組出 SQL Join 語法，好處就是只對資料庫查詢一次、而且是篩選過的資料，在網路傳輸也會比較快一些。</p><pre><code class="language-C#">    // Bad
    public IEnumerable&lt;EpkAccAccount&gt; Find(Expression&lt;Func&lt;EpkAccAccount, bool&gt;&gt; expression)
    {
        return _context.EpkAccAccount.Where(expression);
    }

    // Good
    public IQueryable&lt;EpkAccAccount&gt; FindAsync(Expression&lt;Func&lt;EpkAccAccount, bool&gt;&gt; expression)
    {
        return expression == null ? _context.EpkAccAccount : _context.EpkAccAccount.Where(expression);
    }    
</code></pre><h3>Select 必要欄位</h3><p>使用 SQL 查詢，官方不建議 <code>select * from table</code> 查出所有欄位，只要針對需要欄位就可以。使用 EFCore 也是如此，如果不指定欄位，直接回傳 Entity 就會把所有欄位查詢出來。</p><p>建議在查詢時候，針對必要欄位查詢就可以，才不會造成非必要查詢效能浪費。</p><pre><code class="language-C#">
    // Bad
    var epkAccId = &quot;6006746649&quot;;
    IEnumerable&lt;EpkAccAccount&gt; epkAccAccount = _context.EpkAccAccount.Where(x =&gt; x.EpkAccId == epkAccId).ToListAsync();

    // Good
    var epkAccId = &quot;6006746649&quot;;
    IEnumerable&lt;AccountDto&gt; accountDto = _context.EpkAccAccount
                                        .Where(x =&gt; x.EpkAccId == epkAccId)
                                        .Select(x =&gt; new AccountDto 
                                        { 
                                            EpkAccId = x.EpkAccId, Lpr = x.Lpr, IdNo = x.IdNo 
                                        })
                                        .ToListAsync();

</code></pre><h3>Like 查詢</h3><p>在 EFCore Link 查詢，不建議使用 <code>StartsWith</code>, <code>EndsWith</code>, <code>Contains</code> 這幾個關鍵字查詢，EFCore 並不會轉換成 SQL Like，反而會轉換使用 SQL 函數查詢，雖然查詢結果一樣，但當資料量大時，查詢的效率就會變差。</p><p>在 EFCore 使用 <code>StartsWith</code>, <code>EndsWith</code>, <code>Contains</code> 關鍵字查詢，對應產生 SQL 語法</p><pre><code class="language-SQL">
    -- Contains
    SELECT [traTx].[TRA_TX_ID], [traTx].[CHANNEL_CODE], [traTx].[TRANSACTION_TIME]
    FROM [EPK_ACC_TRA_TX] AS [traTx]
    WHERE CHARINDEX(&#x27;P96&#x27;, [traTx].[CHANNEL_CODE]) &gt; 0 OR &#x27;P96&#x27; = N&#x27;&#x27;

    -- StartsWith
    SELECT [traTx].[TRA_TX_ID], [traTx].[CHANNEL_CODE], [traTx].[TRANSACTION_TIME]
    FROM [EPK_ACC_TRA_TX] AS [traTx]
    WHERE LEFT([traTx].[CHANNEL_CODE], LEN(&#x27;P96&#x27;)) = &#x27;P96&#x27; OR &#x27;P96&#x27; = N&#x27;&#x27;

    -- EndsWith
    SELECT [traTx].[TRA_TX_ID], [traTx].[CHANNEL_CODE], [traTx].[TRANSACTION_TIME]
    FROM [EPK_ACC_TRA_TX] AS [traTx]
    WHERE RIGHT([traTx].[CHANNEL_CODE], LEN(&#x27;P96&#x27;)) = &#x27;P96&#x27; OR &#x27;P96&#x27; = N&#x27;&#x27;

</code></pre><p>如果要 Like 查詢，請要改用 <code>EF.Functions</code> 來查詢。</p><pre><code class="language-C#">    // Bad
    var channelCode = &quot;P96&quot;;
    _context.EpkAccTraTx.Where(x =&gt; x.ChannelCode.StartsWith(channelCode));

    // Good
    var channelCode = &quot;P96&quot;;
    _context.EpkAccTraTx.Where(x =&gt; EF.Functions.Like(x.ChannelCode, $&quot;{channelCode}%&quot;));   
</code></pre><h3>Inner Join</h3><pre><code class="language-C#">
    var smsHistories = _context.EpkAccSmshistory;
    var traTxs = _context.EpkAccTraTx;

    var traNoticeViews = await
    (
        from sms in smsHistories join traTx in traTxs on sms.TraTxId equals traTx.TraTxId
        where traTx.ServiceId == 6 &amp;&amp; traTx.TransactionCode == &quot;01&quot; 
        select new TraNoticeView
        {
            ChannelCode = traTx.ChannelCode,
            OpenFilter = sms.OpenFilter,
            TransactionDate = traTx.TransactionTime.Date,
            Filter = sms.Filter
        }
    ).ToListAsync();

</code></pre><h3>Left Join</h3><pre><code class="language-C#">
    var smsHistories = _context.EpkAccSmshistory;
    var traTxs = _context.EpkAccTraTx;

    var traNoticeViews = await
    (
        from sms in smsHistories join traTx in traTxs on sms.TraTxId equals traTx.TraTxId into histories
        from sms in histories.DefaultIfEmpty()
        where traTx.ServiceId == 6 &amp;&amp; traTx.TransactionCode == &quot;01&quot; 
        select new TraNoticeView
        {
            ChannelCode = traTx.ChannelCode,
            OpenFilter = sms.OpenFilter,
            TransactionDate = traTx.TransactionTime.Date,
            Filter = sms.Filter
        }
    ).ToListAsync();

</code></pre><h3>Group By</h3><pre><code class="language-C#">    var smsHistories = _context.EpkAccSmshistory;
    var traTxs = _context.EpkAccTraTx;

    var traNoticeViews = await
    (
        from sms in smsHistories join traTx in traTxs on sms.TraTxId equals traTx.TraTxId into histories
        from sms in histories.DefaultIfEmpty()
        where traTx.ServiceId == 6 &amp;&amp; traTx.TransactionCode == &quot;01&quot; 
        select new TraNoticeView
        {
            ChannelCode = traTx.ChannelCode,
            OpenFilter = sms.OpenFilter,
            TransactionDate = traTx.TransactionTime.Date,
            Filter = sms.Filter
        }
    )
    .GroupBy(x =&gt; new {x.TransactionDate, x.Filter})
    .ToDictionaryAsync(x =&gt; x.Key, x =&gt; new TraNoticeArrivalCount
    {
        TransactionDate = x.Key.TransactionDate,
        Filter = x.Key.Filter,
        ArrivalCount = x.Count()
    });
                             
</code></pre><h3>EFCore WithNoLock</h3><p>在 MS SQL 下 Select 語法，通常會建議加上<code>WithNoLock</code>，避免 Select 資料表，同時間該資料正在異動中，暫時性的 Lock 等待作業完成期間，造成使用者連線 Timeout 的問題。</p><p>雖然 EFCore 不支援自動補上 WithNoLock 的語法，但可以透過<code>IsolationLevel</code>（交易隔離等級）達到同樣的效果。</p><p>隔離等級有分這幾種，各個差異如下（預設為ReadCommitted）：
| 隔離等級 | 說明 |
| -- | -- |
| Chaos | 無法覆寫來自隔離程度更深之交易的暫止變更 |
| ReadCommitted | 在交易期間無法讀取 Volatile 資料，但可以修改該資料 |
| ReadUncommitted | 在交易期間可以讀取和修改 Volatile 資料。 |
| RepeatableRead | 在交易期間可以讀取 Volatile 資料，但無法修改該資料。 在交易期間可以加入新資料。 |
| Serializable | 在交易期間可以讀取 Volatile 資料，但無法修改該資料，且不能加入新資料。 |
| Snapshot | 可以讀取 Volatile 資料。 交易會在修改資料之前，先驗證在最初讀取資料後是否有另一個交易已變更該資料。 如果資料已更新，則會引發錯誤， 如此可允許交易回到先前所認可的資料值。當您升級使用 Snapshot 隔離層級建立的交易時，會擲回具有錯誤訊息「無法升級 IsolationLevel 為快照的交易」的 InvalidOperationException。 |
| Unspecified | 使用與指定不同的隔離等級，但無法判斷該等級。 如果設定這個值，會擲回例外狀況。 |</p><p>要避免上述該問題，一般指定隔離等級為 ReadUncommitted。</p><p>在程式裡要使用<code>WithNoLock</code>功能，要先從下載<code>Fetc.Core.EntityFramewrokCore</code>套件，只要回傳資料類型是<code>IQueryable</code>就可以使用，使用範例如下。</p><pre><code class="language-C#">
    var repository = _epkUnitOfWork.GetRepository&lt;EpkExcWhitelist&gt;();
    var epkWhitelistDtos = repository.GetAll(x =&gt; x.UploadDate &gt; startDate &amp;&amp; x.UploadDate &lt;= endDate)
                                     .ToListWithNoLock();

</code></pre><h2>SaveChanges</h2><p>EFCore 是透過<code>DbContext.DbSet&lt;T&gt;</code>對資料進行Add、Update、Remov，當下還不會寫入資料庫，而是延遲到<code>SaveChanges</code>後才會真正寫入資料庫，而每次呼叫<code>SaveChanges</code>時，多筆資料異動指令會自動包成一個 Transaction，任一個動作失敗均會觸發 Rollback。</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Produces Attribute 全域設定]]></title>
            <link>https://reantoilpc.github.io/blog/2022-08-02-global-produces-attribute</link>
            <guid>2022-08-02-global-produces-attribute</guid>
            <pubDate>Tue, 02 Aug 2022 00:00:00 GMT</pubDate>
            <description><![CDATA[Application model]]></description>
            <content:encoded><![CDATA[<h2>Application model</h2><p>Application Model 定義 MVC 應用中各種對象和行為，包含Application、Controller、Action、Parameter、Router、Page、Property、Filter等等，上述的功能可以透過<code>慣例</code>(convention)來擴充客製自己的需求，並設定全域或屬性來使用</p><p>ASP.NET Core MVC 透過 IApplicationModelProvider 定義擴充的類別來載入應用程式，官方建議沒有特別需求，盡量不使用<code>provider pattern</code>而是使用<code>conventions</code>進行功能擴充</p><h2>實作 ProduceResponseTypeModelConvention</h2><p>在網路上查到的程式範例都是繼承<code>IApplicationModelProvider</code>因此我改用<code>IApplicationModelConvention</code>來實作，範例如下</p><pre><code class="language-C#">
public class ProduceResponseTypeModelConvention : IApplicationModelConvention
{
    public void Apply(ApplicationModel application)
    {
        foreach (var controller in application.Controllers)
        {
            foreach (var action in controller.Actions)
            {
                Type? returnType = null;
                if (action.ActionMethod.ReturnType.GenericTypeArguments.Any())
                {
                    if (action.ActionMethod.ReturnType.GenericTypeArguments[0].GetGenericArguments().Any())
                    {
                        returnType = action.ActionMethod.ReturnType.GenericTypeArguments[0].GetGenericArguments()[0];
                    }
                }

                AddUniversalStatusCodes(action, returnType);
                
                var actionParametersExist = action.Parameters.Any();
                if (actionParametersExist)
                {
                    AddProducesResponseTypeAttribute(action, typeof(void), StatusCodes.Status404NotFound);
                }
            }
        }

        void AddUniversalStatusCodes(ActionModel action, Type? returnType)
        {
            AddProducesResponseTypeAttribute(action, returnType, StatusCodes.Status200OK);
            AddProducesResponseTypeAttribute(
                action, typeof(ExceptionResponse), StatusCodes.Status500InternalServerError);
        }

        void AddProducesResponseTypeAttribute(ActionModel action, Type? returnType, int statusCodeResult)
        {
            if (returnType != null)
            {
                action.Filters.Add(new ProducesResponseTypeAttribute(returnType, statusCodeResult));
            }
            else if (returnType is null)
            {
                action.Filters.Add(new ProducesResponseTypeAttribute(statusCodeResult));
            }
        }
    }
}

</code></pre><pre><code class="language-C#">
// 擴充功能注入
service.AddControllers(options =&gt;
{
    options.Conventions.Add(new ProduceResponseTypeModelConvention());
})

</code></pre><h2>參考資料來源</h2><p><a href="https://docs.microsoft.com/en-us/aspnet/core/mvc/controllers/application-model?view=aspnetcore-6.0">官方文件說明</a>
<a href="https://www.cnblogs.com/hohoa/p/12134019.html">在Asp.Net Core中使用ModelConvention实现全局过滤器隔离</a>
<a href="https://stackoverflow.com/questions/58047020/net-core-api-make-producesresponsetype-global-parameter-or-automate">Net Core API: Make ProducesResponseType Global Parameter or Automate</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Enums.NET 使用]]></title>
            <link>https://reantoilpc.github.io/blog/2022-07-21-nuget-package-Enums.NET</link>
            <guid>2022-07-21-nuget-package-Enums.NET</guid>
            <pubDate>Thu, 21 Jul 2022 00:00:00 GMT</pubDate>
            <description><![CDATA[使用原因]]></description>
            <content:encoded><![CDATA[<h2>使用原因</h2><p>最近在協助團隊在舊的專案底下開發新 API 功能，該功能是從資料庫取得資料後，經過過濾和整理再回傳前端頁面。
目前遇到一個小問題，該資料的某個欄位存的是<code>A</code>、<code>D</code>、<code>U</code>在前端頁面顯示分別新增、刪除、修改，在顯示上都需要<code>HardCode</code>處理，這樣的寫法造成程式碼的<code>壞味道（Bad Smell）</code>出現。</p><pre><code class="language-C#">
if (actionStatus == &quot;A&quot;) { retrun &quot;新增&quot;; }
if (actionStatus == &quot;D&quot;) { retrun &quot;刪除&quot;; }
if (actionStatus == &quot;U&quot;) { retrun &quot;更新&quot;; }

</code></pre><h2>壞味道（Bad Smell）</h2><p>為什麼HardCode方式，會有壞味道（Bad Smell）出現呢?
因為當<code>actionStatus</code>又增加一種類型，例如<code>Q</code>查詢，勢必就要加一行程式，這樣就違反開放封閉原則</p><h2>改善方式</h2><p>即然要解決HardCode問題，就要把<code>A</code>、<code>D</code>、<code>U</code>的定義要移到另一個Class或是Enum上面，而不是散落在各程式碼裡。目前想到有二個套件，可以解決我的問題</p><ol><li><a href="https://github.com/ardalis/SmartEnum">SmartEnum</a></li><li><a href="https://github.com/TylerBrinkley/Enums.NET">Enums.NET</a></li></ol><p>後來決定使用Enums.NET，原因是</p><ul><li>因為舊的專案已經在使用Enum，如果導入<code>SmartEnum</code>新的做法造成維護上的混亂。</li><li>Enums.NET比較類似原有的Enum的使用方式，比較容易理解。</li></ul><h2>先建立<code>SymbolAttribute</code>和<code>ActionStatusEnum</code></h2><pre><code class="language-C#">
// SymbolAttribute
[AttributeUsage(AttributeTargets.Field)]
public class SymbolAttribute : Attribute
{
    public string Symbol { get; }

    public SymbolAttribute(string symbol)
    {
        Symbol = symbol;
    }
}

// ActionStatusEnum
public enum ActionStatusEnum
{
    [Symbol(&quot;A&quot;)]
    [Description(&quot;新增&quot;)]
    Add,

    [Symbol(&quot;D&quot;)]
    [Description(&quot;刪除&quot;)]
    Delete,

    [Symbol(&quot;U&quot;)]
    [Description(&quot;更新&quot;)]
    Update
}

</code></pre><h2>使用<code>Enums.NET</code>取得文字說明</h2><pre><code class="language-C#">
// Get SymbolAttribute Format
var symbolFormat = Enums.RegisterCustomEnumFormat(x =&gt; x.Attributes.Get&lt;SymbolAttribute&gt;()?.Symbol);

// Return Description
if (Enums.TryParse&lt;ActionStatusEnum&gt;(actionStatus, false, out var status, symbolFormat))
{
    return status.AsString(EnumFormat.Description);
}

</code></pre>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Postman 環境變數設定]]></title>
            <link>https://reantoilpc.github.io/blog/postman-environment-variable</link>
            <guid>postman-environment-variable</guid>
            <pubDate>Tue, 31 May 2022 00:00:00 GMT</pubDate>
            <description><![CDATA[Postman Environment Variable 簡介]]></description>
            <content:encoded><![CDATA[<h2>Postman Environment Variable 簡介</h2><p>Postman 是一套測試 API 工具，藉著 Environment Variable 的設定，可以很方便切換環境，進行 API 功能驗證，概念很像 asp.net 的 appsettings，只要設定好要置換的內容，就可以使用。</p><h2>Environment Variable 設定</h2><ul><li>在 Environment 設定不同環境</li></ul><p><img src="./pictures/2022-05-31/2022-05-31-environment-variable-01.png" alt="image"/></p><ul><li>設定 Variable Name 和 Value</li></ul><p><img src="./pictures/2022-05-31/2022-05-31-environment-variable-02.png" alt="image"/></p><ul><li>使用 Variable 取代原本 Api Url</li></ul><p><img src="./pictures/2022-05-31/2022-05-31-environment-variable-03.png" alt="image"/></p><h2>透過 Response 回寫 Environment Variable</h2><p>使用 Postman 測試 API 會要使用 AccessToken 進行權限的驗證，因此在測試前需要先登入取得 AccessToken 然後複製貼上 Auth 對應欄位，有幾支 API 就要複製幾次，非常麻煩，因此可以透過 Environment Variable 簡化重複貼上的動作。</p><ul><li>先設定 AccessToken 變數，放在 Auth 對應欄位</li></ul><p><img src="./pictures/2022-05-31/2022-05-31-environment-variable-04.png" alt="image"/></p><ul><li>在取得 AccessToken API 的 Tests 裡輸入 Javascript 語法，當成功取得 AccessToken 回寫 Environment Variable</li></ul><p><img src="./pictures/2022-05-31/2022-05-31-environment-variable-05.png" alt="image"/></p><pre><code class="language-javascript">var jsonData = JSON.parse(responseBody);
pm.environment.set(&quot;AccessToken&quot;, jsonData);
</code></pre>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Welcome]]></title>
            <link>https://reantoilpc.github.io/blog/welcome</link>
            <guid>welcome</guid>
            <pubDate>Thu, 26 Aug 2021 00:00:00 GMT</pubDate>
            <description><![CDATA[Docusaurus blogging features are powered by the blog plugin.]]></description>
            <content:encoded><![CDATA[<p><a href="https://docusaurus.io/docs/blog">Docusaurus blogging features</a> are powered by the <a href="https://docusaurus.io/docs/api/plugins/@docusaurus/plugin-content-blog">blog plugin</a>.</p><p>Simply add Markdown files (or folders) to the <code>blog</code> directory.</p><p>Regular blog authors can be added to <code>authors.yml</code>.</p><p>The blog post date can be extracted from filenames, such as:</p><ul><li><code>2019-05-30-welcome.md</code></li><li><code>2019-05-30-welcome/index.md</code></li></ul><p>A blog post folder can be convenient to co-locate blog post images:</p><p><img src="./docusaurus-plushie-banner.jpeg" alt="Docusaurus Plushie"/></p><p>The blog supports tags as well!</p><p><strong>And if you don&#x27;t want a blog</strong>: just delete this directory, and use <code>blog: false</code> in your Docusaurus config.</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[MDX Blog Post]]></title>
            <link>https://reantoilpc.github.io/blog/mdx-blog-post</link>
            <guid>mdx-blog-post</guid>
            <pubDate>Sun, 01 Aug 2021 00:00:00 GMT</pubDate>
            <description><![CDATA[Blog posts support Docusaurus Markdown features, such as MDX.]]></description>
            <content:encoded><![CDATA[<p>Blog posts support <a href="https://docusaurus.io/docs/markdown-features">Docusaurus Markdown features</a>, such as <a href="https://mdxjs.com/">MDX</a>.</p><p>:::tip</p><p>Use the power of React to create interactive blog posts.</p><pre><code class="language-js">&lt;button onClick={() =&gt; alert(&#x27;button clicked!&#x27;)}&gt;Click me!&lt;/button&gt;
</code></pre><button>Click me!</button><p>:::</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Long Blog Post]]></title>
            <link>https://reantoilpc.github.io/blog/long-blog-post</link>
            <guid>long-blog-post</guid>
            <pubDate>Wed, 29 May 2019 00:00:00 GMT</pubDate>
            <description><![CDATA[This is the summary of a very long blog post,]]></description>
            <content:encoded><![CDATA[<p>This is the summary of a very long blog post,</p><p>Use a <code>&lt;!--</code> <code>truncate</code> <code>--&gt;</code> comment to limit blog post size in the list view.</p><p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit amet</p><p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit amet</p><p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit amet</p><p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit amet</p><p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit amet</p><p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit amet</p><p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit amet</p><p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit amet</p><p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit amet</p><p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit amet</p><p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit amet</p><p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit amet</p><p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit amet</p><p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit amet</p><p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit amet</p><p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit amet</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[First Blog Post]]></title>
            <link>https://reantoilpc.github.io/blog/first-blog-post</link>
            <guid>first-blog-post</guid>
            <pubDate>Tue, 28 May 2019 00:00:00 GMT</pubDate>
            <description><![CDATA[Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit amet]]></description>
            <content:encoded><![CDATA[<p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit amet</p>]]></content:encoded>
        </item>
    </channel>
</rss>